'use strict'

import Categories from './categories';
import Transactions from './transactions';
import Budget from './budget';

/**
 * This class represents a user. Each user has
 * a list of transactions, a budget, and a list
 * of categories pertaining to their transactions.
 */

export default class User {

}